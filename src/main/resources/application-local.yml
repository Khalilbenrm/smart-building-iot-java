# Activate with --spring.profiles.active=local when running outside Docker
# (e.g. `mvn spring-boot:run` against a locally exposed Kafka/Mongo).
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/iot
  kafka:
    bootstrap-servers: localhost:9092

mqtt:
  broker-url: tcp://localhost:1883
